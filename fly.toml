# fly.toml app configuration file generated for signal-dashboard-crimson-paper-9831 on 2026-05-03T09:00:37+02:00
#
# See https://fly.io/docs/reference/configuration/ for information about how to use this file.
#

app = 'signal-dashboard-crimson-paper-9831'
primary_region = 'fra'

[build]

[processes]
  app = "/app/.venv/bin/python server.py"

[http_service]
  internal_port = 8080
  force_https = true
  auto_stop_machines = 'stop'
  auto_start_machines = true
  min_machines_running = 1
  processes = ['app']

  [[http_service.checks]]
    grace_period = "20s"
    interval = "30s"
    method = "GET"
    path = "/"
    timeout = "15s"

[[vm]]
  memory = '1gb'
  cpu_kind = 'shared'
  cpus = 1
  memory_mb = 1024
